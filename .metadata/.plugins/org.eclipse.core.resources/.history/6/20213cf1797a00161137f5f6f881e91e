package class99;

public class reverseVow {
    HashSet<T> mySet = new HashSet<T>(Arrays.asList(someArray));
    public String reverse(String input) {
        // Write your solution here.
        return input;
    }
}
